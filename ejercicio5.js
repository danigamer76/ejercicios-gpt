// ejercicio5.js

// 1. Crear una función que reciba un nombre y devuelva un saludo personalizado
function saludar(nombre) {
    return "Hola, " + nombre + "! Bienvenido a JavaScript.";
  }
  
  // 2. Crear una función flecha que multiplique dos números
  const multiplicar = (a, b) => a * b;
  
  // 3. Llamar a ambas funciones y mostrar el resultado en la consola
  console.log(saludar("Carlos"));
  console.log("El resultado de la multiplicación es:", multiplicar(5, 3));
  